name: Check
on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkbout@v3
      - name: Insall deps
        run: npm ci
      - name: Lint
        run: npm run lint
